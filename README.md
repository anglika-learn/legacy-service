``` FILES ADDED ```
`1` Docker folder is added.
`2` In Docker folder, docker_centOS.sh, docker_debian.sh, docker_redHat.sh files are added.
`3` install_docker.sh file has also been added. This script will run the appropriate script on the system.

run `./install_docker.sh`
prompt `y` when asked on the terminal.
